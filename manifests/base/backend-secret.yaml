# Placeholder only. Bootstrap workflow overwrites with real DB_PASSWORD from GitHub secret.
# Argo CD ignoreDifferences prevents reverting the data on sync.
apiVersion: v1
kind: Secret
metadata:
  name: backend-db
  namespace: prod
type: Opaque
stringData:
  DB_PASSWORD: "placeholder-replaced-by-bootstrap"
